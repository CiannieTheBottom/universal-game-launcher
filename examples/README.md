Examples folder

- `recipes/` contains sample YAML recipes showing how to download, install, and launch games using `proton` runtime or native runtime.

Use these as templates when adding new games. Include checksums for sources when possible.